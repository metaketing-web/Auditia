# Skydeen Audit Cockpit — variables d'environnement production
# Copier vers /opt/portia-audit/.env sur le serveur (chmod 600)

REQUIRE_AUTH=1
DISABLE_DEFAULT_USER_SEED=1
ENABLE_TEST_AUDITOR=1
DISABLE_SHARED_ICS_TOKEN=1
SESSION_COOKIE_AUTH=1
HIDE_TOKEN_IN_LOGIN_JSON=1
COOKIE_SECURE=1
FORCE_HSTS=1

REQUIRE_TOTP_PILOTAGE=1
# Comptes soumis à la 2FA obligatoire (doit rester pilotage uniquement)
TOTP_ROLES=admin,juliana
MISSION_ACCESS_CODE=changez-moi-code-fort-et-unique

# Mots de passe (obligatoires si création initiale des comptes)
ADMIN_PASSWORD=
JULIANA_PASSWORD=
AUDITEUR_B_PASSWORD=
AUDITEUR_T_PASSWORD=
CABINET_PASSWORD=

# Équipe nominative — préférer MISSION_TEAM_JSON (une ligne JSON) plutôt qu'un fichier
# MISSION_TEAM_JSON=[{"email":"...","name":"...","role":"auditeur_b","password":"..."}]
MISSION_TEAM_FILE=/opt/portia-audit/team-users.json

# Ne pas réécrire les mots de passe à chaque déploiement (défaut: 0)
# FORCE_TEAM_PASSWORD_SYNC=1

# Collecte publique — max dépôts par IP+token (défaut 20/heure)
# COLLECTE_DEPOSIT_MAX=20
# COLLECTE_DEPOSIT_WINDOW_SEC=3600

PLANNING_ICS_SECRET=
BOOTSTRAP_SECRET=

# Antivirus (ClamAV)
# REQUIRE_AV_SCAN=1
# AV_SCAN_TIMEOUT_SEC=45

# OpenAI / Anthropic — clés API serveur uniquement
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# URL maquettes AO PNIPM (optionnel — exposée après login via /api/config)
# PNIPM_REF_BASE=https://votre-demo.skydeen.ai
